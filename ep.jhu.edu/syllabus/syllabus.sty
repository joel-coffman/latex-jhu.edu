
\RequirePackage{color}
\RequirePackage{helvet}
\RequirePackage{microtype}
\RequirePackage{parskip}
\RequirePackage{titlesec}

% geometry -----
% sets page margins
\RequirePackage[
    inner=1in,% toward binding (i.e., left)
    outer=1in,% away from binding
    top=1in,%
    bottom=1in,%
    %includeheadfoot,
    asymmetric% twosided layout where margins are *not* swapped on alternate pages
]{geometry}


\renewcommand{\maketitle}{%
  \thispagestyle{plain}%
  %
  \noindent%
  {%
    \Large\bfseries%
    \textcolor{bluejay}{\@title}\\[2ex]%
    \textcolor{bluejay}{Syllabus}%
  }%
}

\titleformat{\section}%
  [hang]%
  {\large\bfseries\color{bluejay}}%
  {}%
  {0pt}%
  {}%
\titleformat{\subsection}%
  [hang]%
  {\bfseries\color{bluejay}}%
  {}%
  {0pt}%
  {}%

\definecolor{bluejay}{RGB}{0,103,172}

\renewcommand{\familydefault}{\sfdefault}

\newenvironment{instructor}[1]{%
  \newcommand{\contact}[2]{%
    ##1 & ##2\\%
  }%
  \noindent%
  \textbf{#1}\\[0.5ex]%
  \begin{tabular}{@{}ll}%
}{%
  \end{tabular}%
}
